import { BsAlignCenter } from "react-icons/bs";
import { VehiclesContract } from "./Contract/contract";

const SummaryContract = ({contractList}: {contractList: VehiclesContract}) => {
    return (
        <div style={}
            <a>
                Front dev !!!
            </a>
        </div>
    );
};

export default SummaryContract;